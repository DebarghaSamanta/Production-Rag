pymupdf
markdown
beautifulsoup4==4.12.3      # HTML/web page parsing
requests==2.32.3            # HTTP requests for web page ingestion
lxml==5.2.2                 # HTML parser backend for BeautifulSoup
tiktoken
chromadb
pydantic
python-dotenv   
pytest
pytest-cov
openai

# ── Optional / Future Use (commented out until needed) ─────────────────────────
# sentence-transformers==3.0.1   # Local embedding model (no API key needed)
# rank-bm25==0.2.2               # BM25 keyword retrieval for hybrid search
# cohere==5.5.8                  # Reranker API (retrieval/reranker.py)
# fastapi==0.111.0               # Backend API for frontend
# uvicorn==0.30.1                # ASGI server for FastAPI
# streamlit==1.36.0              # Quick frontend prototype